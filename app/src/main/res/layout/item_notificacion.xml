<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp">

    <!-- Foto del usuario -->
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/imgPerfilNotif"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/ic_default_avatar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:src="@drawable/sample_post1"/>

    <!-- Texto de la notificación -->
    <TextView
        android:id="@+id/tvTextoNotificacion"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Jairo-FCFM le gustó tu publicación."
        android:textColor="#000000"
        android:textSize="15sp"
        android:layout_marginStart="12dp"
        app:layout_constraintStart_toEndOf="@id/imgPerfilNotif"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/imgPreviewNotif"
        app:layout_constraintHorizontal_bias="0" />

    <!-- Imagen de la publicación asociada -->
    <ImageView
        android:id="@+id/imgPreviewNotif"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:scaleType="centerCrop"
        android:src="@drawable/sample_post2"
        android:layout_marginEnd="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
